<div>
  {allPokemonsI.map( ({ id, name, hp, attack, defense, speed, height, weight,
  img, types }) => { return (
  <Pokemons
    key="{id}"
    id="{id}"
    name="{name}"
    hp="{hp}"
    attack="{attack}"
    defense="{defense}"
    speed="{speed}"
    height="{height}"
    weight="{weight}"
    img="{img}"
    types="{types}"
  />
  ); } )}
</div>
